# Telegram
aiogram==3.13.1

# Web
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2

# DB
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0

# Cache / Queue
redis==5.2.0
celery[redis]==5.4.0

# Validation / Config
# aiogram 3.13.1 pins pydantic<2.10, so we stay on the 2.9.x line.
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Audio/Video
# Unpinned: YouTube changes format manifests frequently, so an old yt-dlp
# breaks with "Requested format is not available". The Dockerfile forces
# `pip install -U yt-dlp` after requirements.txt to grab the latest on
# every rebuild.
yt-dlp

# Payments
yookassa==3.5.0

# Summarization — handled via httpx against OpenRouter (OpenAI-compatible).
# No SDK needed; keeps provider-switching to a single env var.

# File formats
python-docx==1.1.2

# Logging / metrics
structlog==24.4.0
sentry-sdk[fastapi]==2.19.0
prometheus-client==0.21.0

# Utils
python-multipart==0.0.18
tenacity==9.0.0
